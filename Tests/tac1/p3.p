PROGRAM main( input, output ) ;

  VAR x : INTEGER       ;  
  VAR y : INTEGER       ;
  VAR a, b, c : REAL    ;
   
BEGIN

   x := -6              ;
   y := x * 2 + x * 3   ;
   a := 2.3             ;
   b := ( 3.0 / 9.9 )   ;
   c := a + b           
   
END.