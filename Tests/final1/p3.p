PROGRAM main( input, output );

VAR x : INTEGER;
VAR y : INTEGER;
VAR a, b, c :  INTEGER;

BEGIN
   x := -6;
   y := x * 2 + x * 3;
   a := 10;
   b := (10 * 2);
   c := a + b;
   write (x);
   write(y);
   write(a);
   write(b);
   write(c)

END.
